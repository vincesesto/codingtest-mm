apiVersion: v1
kind: Service
metadata:
  labels:
    app: httpbin
  name: httpbin-service
  namespace: httpbin-dev
spec:
  type: ClusterIP
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: httpbin
status:
  loadBalancer: {}
